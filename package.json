{
  "name": "cron-job",
  "version": "1.0.0",
  "description": "Cron job using Puppeteer on Render",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npx puppeteer install"
  },
  "dependencies": {
    "puppeteer-core": "^25.0.0"
  }
}
